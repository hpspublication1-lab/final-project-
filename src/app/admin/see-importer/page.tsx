import React from 'react';
import SeeImporterClient from './components/SeeImporterClient';

export const metadata = {
  title: 'Bulk Content Importer — Samyak Guru Admin',
  description: 'Upload and validate SEE Class 10 video course lessons and content in bulk.',
};

export default function SeeImporterPage() {
  return <SeeImporterClient />;
}
